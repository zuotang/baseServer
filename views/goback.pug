<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title 正在返回
        script
            | history.back()
    body
        div 操作成功，正在返回！